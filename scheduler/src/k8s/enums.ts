export enum K8sClientTypeApi {
  APPS = 'apps',
  CORE = 'core',
  OBJECTS = 'objects',
  METRICS = 'metrics',
}

export enum MetricsType {
  CPU = 'cpu',
  MEMORY = 'memory',
  CPU_MEMORY = 'cpu-memory',
  LATENCY = 'latency',
}
